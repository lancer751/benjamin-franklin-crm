{
  "name": "moodle-enroll-manager",
  "version": "0.0.1",
  "description": "",
  "packageManager": "bun@1.2.4",
  "workspaces": [
    "./backend",
    "./management-dashboard",
    "./shared",
    "./commercial-website",
    "./packages/**"
  ],
  "scripts": {
    "dev": "turbo dev",
    "dev:client": "turbo dev --filter=management-dashboard",
    "dev:server": "turbo dev --filter=backend",
    "build": "turbo build",
    "build:client": "turbo build --filter=management-dashboard",
    "build:server": "turbo build --filter=backend",
    "lint": "turbo lint",
    "type-check": "turbo type-check",
    "test": "turbo test",
    "postinstall": "turbo build --filter=@repo/database --filter=shared --filter=backend"
  },
  "author": "lauwc",
  "license": "ISC",
  "type": "module",
  "private": true,
  "devDependencies": {
    "bun-types": "latest",
    "turbo": "^2.6.3"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=22.12.0"
  }
}